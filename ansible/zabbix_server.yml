---
# Playbook: Configura Zabbix Server
# Executa na instância com tag Role=zabbix-server

- name: Configurar Zabbix Server
  hosts: zabbixserver   # grupo gerado pelo inventário dinâmico (tag Role)
  gather_facts: true
  become: true

  roles:
    - role: zabbix_server
